# Ralphy Configuration
# https://github.com/michaelshimeles/ralphy

# Project info
project:
  name: "go-intl"
  language: "Go"
  framework: ""
  description: "A Go library"

# Commands
commands:
  test: "task test"
  lint: "task lint"
  build: "go build ./..."
  verify: "task verify"

# Rules - instructions the AI MUST follow
# These are injected into every prompt
rules:
  - "Follow SOLID, KISS (Keep It Simple, Stupid), DRY (Don't Repeat Yourself), and YAGNI (You Ain't Gonna Need It) principles."
  - "Before writing code, read at least two relevant reference implementations from .reference/ to understand design trade-offs."
  - "All tests must pass with -race flag. Target >80% test coverage with edge cases."
  - "Use pre-compiled regex patterns at package level and pre-allocate slices/maps when size is predictable."
  - "Follow [Google Go Best Practices](https://google.github.io/go-style/best-practices)"
  - "Follow [Google Go Decisions](https://google.github.io/go-style/decisions)"
  - "Follow [Google Go Formatting](https://google.github.io/go-style/formatting)"

# Boundaries - files/folders the AI should not modify
boundaries:
  never_touch:
    - "bin/**"
    - "go.sum"
